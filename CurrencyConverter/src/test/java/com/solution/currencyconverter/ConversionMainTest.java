package com.solution.currencyconverter;

import com.solution.currencyconverter.datatypes.Coin;
import com.solution.currencyconverter.enums.Coins;
import org.junit.Assert;
import org.junit.Test;

import java.math.BigDecimal;


class ConversionMainTest {
/*



    void currencyConvert() {
        CoinFactory coinFactory = new CoinFactory();
         Coins itsValue = Coins.fromPlaceInList(1);
        BigDecimal itsAmount = BigDecimal.valueOf(24);
        Coin selectedCoin = null;
        try {
            selectedCoin = coinFactory.getCon(itsValue);
        } catch (Exception e) {
            e.printStackTrace();
        }
        double result = selectedCoin.calculate(itsAmount.doubleValue());
        double expectedResult = 34;
        Assert.assertEquals("Wrong amount of the conversion to ILS",expectedResult,result);

    }*/
}